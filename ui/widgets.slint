import { Theme } from "theme.slint";

export component UiText inherits Text {
    font-family: "Inter";
    font-size: 13px;
    font-weight: 500;
    color: Theme.on_surface;
    letter-spacing: 0px;
}
